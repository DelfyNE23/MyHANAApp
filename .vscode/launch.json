{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "cds watch --profile hybrid",
      "name": "debug secion with cds",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}",
      /*"runtimeExecutable": "cds",
      "args": [
       "serve",
        "--with-mocks",
        "--in-memory?"
      ],
      "skipFiles": [
        "<node_internals>/**"
      ]*/
    },
    {
      "command": "cds bind --exec npm run app",
      "name": "approuter injection",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}",
    }
  ]
}
